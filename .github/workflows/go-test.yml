name: go test

permissions:
  pull-requests: read
  contents: read

on:
  push:
    branches:
      - master

  pull_request:

jobs:
  test:
    uses: go-openapi/ci-workflows/.github/workflows/go-test-monorepo.yml@f3de433eeb102475ff4f5c5b2f19e25af8731ad0 # v0.3.2
    secrets: inherit
